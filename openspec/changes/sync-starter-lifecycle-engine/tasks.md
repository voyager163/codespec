## 1. Build the sync script

- [x] 1.1 Create `scripts/sync-starter-lifecycle.js`, modeled on `desktop/scripts/sync-lifecycle.js`: a pure whole-tree `fs.cpSync(tools/lifecycle, templates/starter/tools/lifecycle, { recursive, filter })` with the runtime-dir `SKIP` regex. Export `sync()` and `drift()`.
- [x] 1.2 No content exclusion list needed: the one dev-only concern (the "desktop 'Create a new app' lands the full starter" block in `selftest.js`) is guarded at runtime with `if (starterDir())` — `starterDir()` is already `null` in a scaffolded app — so the identical file is correct in both trees and `cpSync`'s file-level filter (which can't exclude a block inside a file) is not needed.
- [x] 1.3 Wire the script into `package.json` (`"sync:starter": "node scripts/sync-starter-lifecycle.js"`).

## 2. Run the sync and verify the gap closes

- [x] 2.1 Run the sync script once against the current `tools/lifecycle/` (53 files written).
- [x] 2.2 Confirm `templates/starter/tools/lifecycle/` now has `preview.js`, `pac-init.js`, and `dataverse-schema.js`.
- [x] 2.3 Confirm `bin/powercodex-lifecycle.js` in the starter now has the `dataverse`/`code-init`/`code-push` subcommands and `--fix-mode`/`--max-heal-retries` flags.
- [x] 2.4 Confirm prior hand-edits are superseded, not lost: `drift()` returns `[]` — every starter lifecycle file is now byte-identical to its dev twin, and dev already carries the equivalent theater-removal / `pac code push` fixes.

## 3. Add the drift guard

- [x] 3.1 In `tools/lifecycle/lib/selftest.js`, add a check that re-runs the sync's compare logic in-memory (`drift()` from the sync script) and fails if the on-disk `templates/starter/tools/lifecycle/` tree differs. Guarded on the sync script's existence so it self-skips in a scaffolded/vendored copy.
- [x] 3.2 Confirmed: the check passes on a freshly-synced tree (dev selftest 170/170) and names drifted files when the starter is edited out of sync (the check message interpolates `drift()`'s file list).

## 4. Verify

- [x] 4.1 Ran `npm run lifecycle:selftest` (dev copy) — **PASS 170/170**, including the new drift-guard check.
- [x] 4.2 Ran the starter's own selftest (`node bin/powercodex-lifecycle.js selftest` from `templates/starter/tools/lifecycle/`) — **PASS 158/158, exit 0** (12 fewer than dev: the dev-only scaffold block, drift guard, and vendored-starter canvas checks self-skip), no crash on the previously-missing pac-init/preview modules.
- [x] 4.3 Documented in `tools/lifecycle/README.md` that `templates/starter/tools/lifecycle/` (and `desktop/vendor/lifecycle/`) are generated by `scripts/sync-starter-lifecycle.js` — do not hand-edit.
- [x] 4.4 Ran `openspec validate sync-starter-lifecycle-engine` — valid.
